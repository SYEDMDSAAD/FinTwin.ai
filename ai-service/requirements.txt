# Pinned for reproducible installs. Versions chosen to resolve cleanly together
# (the previously hand-built venv had scipy 1.17.1, which violates statsforecast's
# scipy<1.16 requirement, and was missing the OCR deps entirely).
python-dotenv==1.2.2
requests==2.34.2

# ── Web framework ────────────────────────────────────────────────────────────
fastapi==0.136.3
starlette==1.1.0
pydantic==2.13.4
pydantic_core==2.47.0
uvicorn==0.48.0
anyio==4.13.0
h11==0.16.0
idna==3.16
click==8.4.1
python-multipart==0.0.29   # required by FastAPI for the OCR file upload (File/Form)

# ── Data / ML ────────────────────────────────────────────────────────────────
numpy==2.4.6
pandas==2.3.3
scipy==1.15.3            # <1.16 to satisfy statsforecast
scikit-learn==1.8.0
statsforecast==2.0.3
xgboost==3.2.0
numba==0.65.1
llvmlite==0.47.0
joblib==1.5.3
threadpoolctl==3.6.0

# ── OCR (were missing — required by ocr/ocr_engine.py) ───────────────────────
pillow==12.2.0
pytesseract==0.3.13

# ── Market data (were missing — required by investments/price_service.py) ────
yfinance==1.4.1

# ── Shared transitives ───────────────────────────────────────────────────────
python-dateutil==2.9.0.post0
six==1.17.0
typing-inspection==0.4.2
typing_extensions==4.15.0
annotated-doc==0.0.4
annotated-types==0.7.0
