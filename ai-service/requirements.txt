# Pinned for reproducible installs. Versions chosen to resolve cleanly together
# (the previously hand-built venv had scipy 1.17.1, which violates statsforecast's
# scipy<1.16 requirement, and was missing the OCR deps entirely).
python-dotenv==1.2.2
requests==2.34.2
prometheus-client==0.24.1   # /metrics endpoint + AI guardrail counters

# ── Web framework ────────────────────────────────────────────────────────────
fastapi==0.138.2
starlette==1.3.1
pydantic==2.13.4
pydantic_core==2.46.4
uvicorn==0.48.0
anyio==4.14.2       # CVE-2026-63374 (CRITICAL), CVE-2026-63349 (HIGH) fixed in 4.14.2
h11==0.16.0
idna==3.16
click==8.4.2
python-multipart==0.0.30   # required by FastAPI for the OCR file upload (File/Form)

# ── Data / ML ────────────────────────────────────────────────────────────────
numpy==2.4.6
pandas==2.3.3
scipy==1.15.3            # <1.16 to satisfy statsforecast
statsforecast==2.0.3
statsmodels==0.14.6      # statsforecast dependency
numba==0.66.0
llvmlite==0.48.0   # must move in lockstep with numba (numba pins llvmlite's minor)
threadpoolctl==3.6.0

# ── OCR (were missing — required by ocr/ocr_engine.py) ───────────────────────
pillow==12.3.0      # 11 HIGH CVEs (heap OOB write, decompression-bomb bypass) fixed in 12.3.0
pytesseract==0.3.13

# ── Market data (were missing — required by investments/price_service.py) ────
yfinance==1.4.1

# ── Statement import: PDF / Excel → grid (statements/extract.py) ─────────────
pdfplumber==0.11.10
pdfminer.six==20260107     # pdfplumber's text layer; opens AES-encrypted bank PDFs
pypdfium2==5.13.0
cryptography==50.0.1       # pdfminer.six: AES decryption for password-protected PDFs
cffi==2.0.0
pycparser==3.0
charset-normalizer==3.4.7
openpyxl==3.1.5
et-xmlfile==2.0.0
defusedxml==0.7.1          # openpyxl picks it up automatically: blocks XML entity-expansion bombs
xlrd==2.0.2                # legacy .xls (BIFF)
lxml==6.1.3                # HTML tables saved with an .xls extension

# ── Shared transitives ───────────────────────────────────────────────────────
python-dateutil==2.9.0.post0
six==1.17.0
typing-inspection==0.4.2
typing_extensions==4.15.0
annotated-doc==0.0.4
annotated-types==0.7.0
