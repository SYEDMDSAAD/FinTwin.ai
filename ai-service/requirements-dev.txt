# Test-only dependencies (not needed at runtime).
-r requirements.txt
pytest==8.3.4
httpx==0.28.1
fpdf2==2.8.8   # builds PDF statement fixtures (incl. encrypted) in tests
xlwt==1.3.0    # builds legacy .xls fixtures in tests
