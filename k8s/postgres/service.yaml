apiVersion: v1
kind: Service
metadata:
  name: postgres-service
  namespace: fintwin
spec:
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
  # ClusterIP — only reachable inside the cluster. Never expose PostgreSQL externally.
  type: ClusterIP
