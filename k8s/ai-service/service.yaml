apiVersion: v1
kind: Service
metadata:
  name: ai-service
  namespace: fintwin
spec:
  selector:
    app: ai-service
  ports:
    - port: 8000
      targetPort: 8000
  # ClusterIP — only the backend can call the AI service. Never expose publicly.
  type: ClusterIP
