apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: fintwin
spec:
  selector:
    app: backend
  ports:
    - port: 8080
      targetPort: 8080
  # ClusterIP — Nginx/Ingress forwards /api/* to this service. Never expose directly.
  type: ClusterIP
