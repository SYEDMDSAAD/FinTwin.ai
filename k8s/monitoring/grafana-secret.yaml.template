apiVersion: v1
kind: Secret
metadata:
  name: grafana-secret
  namespace: fintwin
type: Opaque
stringData:
  # Change before applying: kubectl apply -f grafana-secret.yaml
  admin-password: "CHANGE_ME_STRONG_PASSWORD"
